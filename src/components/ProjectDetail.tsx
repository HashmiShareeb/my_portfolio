import React from "react";

export default ({
  title,
  image,
  url,
}: {
  title: string;
  image: string;
  url: string;
}) => {
  return (
    <section className="py-20">
      <h1>test</h1>
    </section>
    
  
  );
};
